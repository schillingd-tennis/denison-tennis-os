/**
 * Contact Workflow v0.1 configuration (Today Beta).
 */
export const TODAY_BETA_SOURCE_SYSTEM = "today_beta";

/** Days to suppress cadence-only opportunities after Snooze. */
export const CADENCE_SNOOZE_DAYS = 3;
