import RankingsPlaceholderWorkspace from "@/features/rankings/components/RankingsPlaceholderWorkspace";

export default function CurrentNpiRankingsPage() {
  return (
    <RankingsPlaceholderWorkspace
      activeId="current-npi"
      description="Current NPI rankings have not been imported into the OS yet."
    />
  );
}
