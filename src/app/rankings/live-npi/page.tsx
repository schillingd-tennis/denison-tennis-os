import RankingsPlaceholderWorkspace from "@/features/rankings/components/RankingsPlaceholderWorkspace";

export default function LiveNpiRankingsPage() {
  return (
    <RankingsPlaceholderWorkspace
      activeId="live-npi"
      description="Live NPI ranking updates are not available in the OS yet."
    />
  );
}
