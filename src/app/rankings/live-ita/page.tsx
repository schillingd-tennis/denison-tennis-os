import RankingsPlaceholderWorkspace from "@/features/rankings/components/RankingsPlaceholderWorkspace";

export default function LiveItaRankingsPage() {
  return (
    <RankingsPlaceholderWorkspace
      activeId="live-ita"
      description="Live ITA ranking updates are not available in the OS yet."
    />
  );
}
